name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: yarn
      - run: yarn install --frozen-lockfile
      - name: Install Next.js app dependencies
        run: cd next-app && yarn install --frozen-lockfile
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-west-2
      - name: Clean up stale log groups
        run: |
          aws logs delete-log-group --log-group-name /aws/lambda/smash-bros-fighters-sync --region us-west-2 2>/dev/null || true
          aws logs delete-log-group --log-group-name /aws/lambda/smash-bros-fighters-backfill --region us-west-2 2>/dev/null || true
      - name: Deploy CDK stack
        run: npx cdk deploy --require-approval never
